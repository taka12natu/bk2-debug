<%= form_with model: chat do |f| %>
  	<%= f.text_field :chat_messege, :placeholder => "メッセージを入力して下さい" , :size => 70, id: "margin" %>
  	<%= f.hidden_field :room_id, :value => room.id %>
  	<br>
  	<%= f.submit "投稿する", id: "margin" %>
<% end %>